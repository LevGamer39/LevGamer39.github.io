// home.js - page-specific behavior (optional)
console.log('Home page loaded');
// You can add page-specific terminal commands by exposing window.TERMINAL_COMMANDS before terminal.js runs,
// but for simplicity we use defaults. If needed, uncomment pattern below:

// window.TERMINAL_COMMANDS = Object.assign(window.TERMINAL_COMMANDS || {}, {
//   welcome() { console.log('custom'); }
// });
